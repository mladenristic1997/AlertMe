<UserControl x:Class="AlertMe.Home.PlanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:progress="clr-namespace:AlertMe.Timeline;assembly=AlertMe.Timeline"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid
        Width="Auto"
        Height="Auto"
        Name="Wrapper"
        >
        <progress:Progress 
            Alerts="{Binding Alerts}"
            ControlWidth="{Binding ElementName=Wrapper, Path=ActualWidth,UpdateSourceTrigger=PropertyChanged}"
            PlanDuration="{Binding PlanDuration}"
            />
    </Grid>
</UserControl>
